import { Presence } from '../../common/types/presence.types';
import { ClientConnection } from '../connection';
import { ClientState } from '../connection/types';
import { Room } from '../room';
export declare class Realtime {
    private apiKey;
    private environment;
    private presence;
    private secret?;
    private clientId?;
    private socket;
    private manager;
    connection: ClientConnection;
    constructor(apiKey: string, environment: 'dev' | 'prod', presence: Presence, secret?: string, clientId?: string);
    get state(): ClientState;
    /**
     * @function connect
     * @param room - The room name
     * @param maxConnections - The maximum number of connections allowed in the room
     * @returns {Room} - The room instance
     */
    connect(room: string, maxConnections?: number | 'unlimited'): Room;
    destroy(): void;
}
