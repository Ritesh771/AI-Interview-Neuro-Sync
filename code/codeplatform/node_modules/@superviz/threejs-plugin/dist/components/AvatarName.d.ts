import { Object3D } from 'three';
import * as THREE from 'three';
import SpriteText from 'three-spritetext';
import { Slot } from '../types';
export declare class AvatarName {
    constructor(name: string, slot: Slot, height: number, scale: number);
    model: Object3D;
    private name;
    private color;
    private scale;
    private height;
    slot: Slot;
    text: SpriteText;
    load: () => Promise<Object3D<THREE.Object3DEventMap>>;
    createTextGeometry: (text: string, color: string) => SpriteText;
    destroy: () => void;
}
