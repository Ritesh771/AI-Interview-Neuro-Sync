function r(t) {
  if (t = t.replace("#", ""), t.length === 6) {
    const e = parseInt(t.substring(0, 2), 16), n = parseInt(t.substring(2, 4), 16), s = parseInt(t.substring(4, 6), 16);
    return `${e} ${n} ${s}`;
  } else if (t.startsWith("rgb(") && t.endsWith(")")) {
    const e = t.substring(4, t.length - 1).split(",");
    return `${e[0].trim()} ${e[1].trim()} ${e[2].trim()}`;
  } else
    return t;
}
function i(t) {
  const e = {};
  for (const [n, s] of Object.entries(t))
    e[n] = r(s);
  return e;
}
export {
  i as createTheme
};
