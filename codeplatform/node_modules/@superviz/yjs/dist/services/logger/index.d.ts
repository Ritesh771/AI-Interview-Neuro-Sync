export type Message = string | Error | number | Object;
export declare class Logger {
    private prefix;
    private debug;
    constructor(scope: string, prefix: string);
    log(formatter: Message, ...args: Array<Message>): void;
}
