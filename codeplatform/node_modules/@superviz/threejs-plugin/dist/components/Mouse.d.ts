import { Vector3, Object3D, Scene, Sprite } from 'three';
import SpriteText from 'three-spritetext';
import { Slot } from '../types';
export declare class Mouse {
    constructor(scene: Scene);
    private clock;
    private destPos;
    private curPos;
    private counter;
    slot: Slot;
    private lastSlot;
    private lastDestPos;
    origin: Vector3;
    private scene;
    private lerpAnimationFrame;
    textMesh: Object3D;
    arrowMesh: Object3D;
    root: Object3D;
    private backgroundMaterial;
    load(name: string, slot: Slot): Promise<Object3D>;
    update: any;
    private animate;
    destroy(): void;
    createTextGeometry: (text: string, color: string) => SpriteText;
    createArrowGeometry: (slot: Slot) => Promise<Sprite>;
}
